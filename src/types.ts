// Re-exports for convenience — types live in their respective modules.
export type { question } from "./utils/extractQn";
export type {
  QuizAttempt,
  AppView,
  AppContextType,
} from "./components/AppContext";
